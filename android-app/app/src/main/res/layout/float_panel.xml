<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/float_panel_root"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/transparent">

    <LinearLayout
        android:id="@+id/float_panel_content"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="@drawable/bg_float_panel"
        android:layout_gravity="top|start">

        <LinearLayout
            android:id="@+id/float_header"
            android:layout_width="match_parent"
            android:layout_height="38dp"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:background="@drawable/bg_float_header"
            android:paddingStart="4dp"
            android:paddingEnd="2dp">

            <ImageButton
                android:id="@+id/float_menu"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:background="@android:color/transparent"
                android:src="@drawable/ic_menu"
                android:contentDescription="菜单" />

            <TextView
                android:id="@+id/float_title"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="4dp"
                android:text="小窗"
                android:textColor="@color/text"
                android:textSize="13sp"
                android:textStyle="bold"
                android:maxLines="1"
                android:ellipsize="end" />

            <ImageButton
                android:id="@+id/float_close"
                android:layout_width="32dp"
                android:layout_height="32dp"
                android:background="@android:color/transparent"
                android:src="@drawable/ic_close"
                android:contentDescription="关闭" />
        </LinearLayout>

        <FrameLayout
            android:id="@+id/float_content_area"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1">

            <!-- 聊天记录（与全屏一致的 RecyclerView 气泡界面） -->
            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/float_chat_list"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:padding="4dp" />

            <!-- 菜单浮层 -->
            <LinearLayout
                android:id="@+id/float_menu_overlay"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:background="@color/panel"
                android:visibility="gone">

                <TextView
                    android:id="@+id/float_opt_new"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:background="@drawable/bg_float_option"
                    android:gravity="center_vertical"
                    android:padding="12dp"
                    android:text="＋ 新增对话"
                    android:textColor="@color/text"
                    android:textSize="14sp" />

                <TextView
                    android:id="@+id/float_opt_history"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:background="@drawable/bg_float_option"
                    android:gravity="center_vertical"
                    android:padding="12dp"
                    android:text="🕐 历史对话"
                    android:textColor="@color/text"
                    android:textSize="14sp" />

                <TextView
                    android:id="@+id/float_opt_thinking"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:background="@drawable/bg_float_option"
                    android:gravity="center_vertical"
                    android:padding="12dp"
                    android:text="思考设置：开 · 自动"
                    android:textColor="@color/text"
                    android:textSize="14sp" />

            </LinearLayout>

            <!-- 历史对话列表 -->
            <ScrollView
                android:id="@+id/float_history_view"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:background="@color/panel"
                android:visibility="gone">

                <LinearLayout
                    android:id="@+id/float_history_list"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:orientation="vertical" />
            </ScrollView>
        </FrameLayout>

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:padding="8dp">

            <EditText
                android:id="@+id/float_input"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:background="@drawable/bg_input"
                android:hint="输入消息…"
                android:inputType="textMultiLine"
                android:maxLines="4"
                android:padding="6dp"
                android:textColor="@color/text"
                android:textColorHint="@color/muted"
                android:textSize="13sp" />

            <ImageButton
                android:id="@+id/float_send"
                android:layout_width="42dp"
                android:layout_height="42dp"
                android:layout_marginStart="8dp"
                android:background="@drawable/bg_accent_round"
                android:src="@drawable/ic_send"
                android:contentDescription="发送" />

            <TextView
                android:id="@+id/float_resize"
                android:layout_width="20dp"
                android:layout_height="20dp"
                android:layout_marginStart="4dp"
                android:gravity="center"
                android:text="◢"
                android:textColor="@color/muted"
                android:textSize="12sp" />
        </LinearLayout>
    </LinearLayout>
</FrameLayout>
